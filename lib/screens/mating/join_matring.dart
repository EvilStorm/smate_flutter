import 'package:flutter/material.dart';

class JoinMating extends StatelessWidget {
  const JoinMating({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Join'),
    );
  }
}
