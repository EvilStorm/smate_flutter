import 'package:flutter/material.dart';

class InterestingMate extends StatelessWidget {
  const InterestingMate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('intertesting'),
    );
  }
}
