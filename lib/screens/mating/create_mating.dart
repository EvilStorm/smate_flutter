import 'package:flutter/material.dart';

class CreateMating extends StatelessWidget {
  const CreateMating({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('creating'),
    );
  }
}
