import 'package:flutter/material.dart';

class InterFereHome extends StatelessWidget {
  const InterFereHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('This is interfere page')),
    );
  }
}
